name: 'test-arguments-action'
description: 'Test arguments action'
author: 'Abhilash Nair'

# Add your action's branding here. This will appear on the GitHub Marketplace.
branding:
  icon: 'heart'
  color: 'red'

# Define your inputs here.
inputs:
  stepsContext:
    description: 'Step context for the workflow'
    required: false

runs:
  using: node20
  main: dist/index.js
